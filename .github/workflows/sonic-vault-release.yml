name: Sonic Vault Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build-windows:
    name: Build Windows
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
      - run: flutter pub get
      - run: flutter build windows --release
      - name: Bundle VC++ runtime DLLs
        run: |
          $dlls = @("vcruntime140.dll","vcruntime140_1.dll","msvcp140.dll","msvcp140_1.dll","msvcp140_2.dll")
          foreach ($dll in $dlls) {
            $src = "C:\Windows\System32\$dll"
            if (Test-Path $src) { Copy-Item $src "build\windows\x64\runner\Release\" -Force }
          }
      - name: Install Inno Setup
        run: choco install innosetup -y
      - name: Build installer
        run: |
          & "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" "installer\installer.iss"
      - name: Sign installer
        if: ${{ secrets.WINDOWS_CERT_PASSWORD != '' }}
        run: |
          signtool sign /fd SHA256 /a /f "cert\sonicvault.pfx" /p $env:WINDOWS_CERT_PASSWORD "installer\SonicVault-Setup-1.2.1.exe"
        env:
          WINDOWS_CERT_PASSWORD: ${{ secrets.WINDOWS_CERT_PASSWORD }}
      - uses: actions/upload-artifact@v4
        with:
          name: SonicVault-windows
          path: installer/SonicVault-Setup-1.2.1.exe

  build-android:
    name: Build Android
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
      - run: flutter pub get
      - run: flutter build apk --release
      - uses: actions/upload-artifact@v4
        with:
          name: SonicVault-android
          path: build/app/outputs/flutter-apk/app-release.apk

  build-linux:
    name: Build Linux
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
      - run: flutter pub get
      - run: |
          sudo apt-get update
          sudo apt-get install -y clang cmake ninja-build libgtk-3-dev
      - run: flutter build linux --release
      - name: Package Linux build
        run: |
          tar -czf SonicVault-linux.tar.gz -C build/linux/x64/release/bundle .
      - uses: actions/upload-artifact@v4
        with:
          name: SonicVault-linux
          path: SonicVault-linux.tar.gz

  build-macos:
    name: Build macOS
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
      - run: flutter pub get
      - run: flutter build macos --release
      - name: Package macOS build
        run: |
          cd build/macos/Build/Products/Release
          zip -r ../../../../../SonicVault-macos.zip sonicvault.app
      - uses: actions/upload-artifact@v4
        with:
          name: SonicVault-macos
          path: SonicVault-macos.zip

  create-release:
    name: Create Release
    needs: [build-windows, build-android, build-linux, build-macos]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
      - name: Create GitHub Release
        run: |
          gh release create "${{ github.ref_name }}" \
            SonicVault-windows/SonicVault-Setup-1.2.1.exe \
            SonicVault-android/app-release.apk \
            SonicVault-linux/SonicVault-linux.tar.gz \
            SonicVault-macos/SonicVault-macos.zip \
            --title "${{ github.ref_name }}" \
            --generate-notes
        env:
          GH_TOKEN: ${{ github.token }}
